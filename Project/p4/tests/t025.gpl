
int b = 1;

int c = !b;
int d = -c;

int e = -b;
int f = !b;

int g = !-b;
int h = -!b;

int i = !(-b);
int j = -(!b);

// ! should have a higher precedence than * resulting in 0
// If the precedences are reverse the result would be 1
int k = !b * 0;

