// this is part of t008.gpl in p6
int x = floor(100 * cos(20)) + 200;
int y = floor(100 * sin(20));

// gpl floor works just like the C++ floor
//  floor(-1.2) == -2.0
int should_be_minus_2 = floor(-1.2);
